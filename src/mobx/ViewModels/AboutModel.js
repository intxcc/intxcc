import DefaultModel from './variants/about/DefaultModel'

// This is the ViewModel of the about page

const ViewModel = {
  name: 'about',
  variants: {
    'default': DefaultModel
  }
}

export default ViewModel
