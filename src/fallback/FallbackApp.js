'use strict'

import React from 'react'
// import PropTypes from 'prop-types'

import { observer } from 'mobx-react'

const FallbackApp = observer(props => (
  <span>
    test
  </span>
))

export default FallbackApp
