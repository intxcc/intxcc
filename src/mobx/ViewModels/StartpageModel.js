import DefaultModel from './variants/startpage/DefaultModel'

// This is the ViewModel of the startpage

const ViewModel = {
  name: 'startpage',
  variants: {
    'default': DefaultModel
  }
}

export default ViewModel
